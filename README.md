# Legacy Files

This branch has no shared history with `main`. It exists solely as a stable
anchor for a GitHub release that carries legacy RIP-era file attachments.

Do not merge this branch into `main`, and do not merge `main` into it.
